import React, { FunctionComponent } from "react";
import { Header } from "semantic-ui-react";

const Homepage: FunctionComponent = () => (
    <Header as="h1" inverted>Homepage</Header>
);

export default Homepage;
